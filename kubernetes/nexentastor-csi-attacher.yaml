# Attacher and CSI driver API objects
# Necessary to run external CSI attacher for NFS

kind: Service
apiVersion: v1
metadata:
  name: nexentastor-csi-attacher
  labels:
    app: nexentastor-csi-attacher
spec:
  selector:
    app: nexentastor-csi-attacher
  ports:
    - name: dummy
      port: 12345

---
kind: StatefulSet
apiVersion: apps/v1beta1
metadata:
  name: nexentastor-csi-attacher
spec:
  serviceName: 'nexentastor-csi-attacher'
  replicas: 1
  template:
    metadata:
      labels:
        app: nexentastor-csi-attacher
    spec:
      serviceAccount: nexentastor-csi-attacher
      containers:
        - name: csi-attacher
          image: quay.io/k8scsi/csi-attacher:v0.4.0
          args:
            - '--v=5'
            - '--csi-address=$(ADDRESS)'
          env:
            - name: ADDRESS
              value: /var/lib/csi/sockets/pluginproxy/csi.sock
          imagePullPolicy: 'Always'
          volumeMounts:
            - name: socket-dir
              mountPath: /var/lib/csi/sockets/pluginproxy/
        - name: driver
          image: 10.3.199.92:5000/nexentastor-csi-plugin:latest
          args:
            - '--nodeid=$(NODE_ID)'
            - '--endpoint=$(CSI_ENDPOINT)'
          env:
            - name: NODE_ID
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: CSI_ENDPOINT
              value: unix://plugin/csi.sock
          imagePullPolicy: 'Always'
          volumeMounts:
            - name: socket-dir
              mountPath: /plugin
            - name: secret
              mountPath: /config
      volumes:
        - name: socket-dir
          ? emptyDir
        - name: secret
          secret:
            secretName: nexentastor-csi-driver-config
