nexentastor_map:
  nstor-box1:
    restIp: https://10.3.199.197:8443               # [required] NexentaStor REST API endpoint(s)
    username: admin                                 # [required] NexentaStor REST API username
    password: Nexenta@1                             # [required] NexentaStor REST API password
    defaultDataset: csiDriverPool/csiDriverDataset  # default dataset for driver's fs/volume [pool/dataset]
    defaultDataIp: 10.3.199.197                     # default NexentaStor data IP or HA VIP
    zone: zone-1
    defaultMountFsType: cifs                                    # default mount fs type [nfs|cifs]
    defaultMountOptions: username=admin,password=Nexenta@1      # default mount options (mount -o ...)

  nstor-box2:
    restIp: https://10.3.199.189:8443               # [required] NexentaStor REST API endpoint(s)
    username: admin                                 # [required] NexentaStor REST API username
    password: Nexenta@1                             # [required] NexentaStor REST API password
    defaultDataset: tank01/fs01                     # default dataset for driver's fs/volume [pool/dataset]
    defaultDataIp: 10.3.199.189                     # default NexentaStor data IP or HA VIP
    zone: zone-2
    defaultMountFsType: cifs                                    # default mount fs type [nfs|cifs]
    defaultMountOptions: username=admin,password=Nexenta@1      # default mount options (mount -o ...)
